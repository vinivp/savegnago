const resetBloco = document.querySelectorId("restaurarTabela");
const bloco = document;querySelectorId("anotacaoTabela")

resetBloco.addEventListener("click", (bloco) => {
    bloco.value = ""
})